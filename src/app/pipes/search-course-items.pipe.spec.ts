import { SearchCourseItemsPipe } from './search-course-items.pipe';

describe('SearchCourseItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCourseItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
